---
import { $ } from "../lib/globals";

const desc = $.desc.toString();
const blogger = $.blogger.toString();
const image = $.image.toString();
const imageDesc = $.blog.image.description.toString();
const blogImage = $.blog.image.toString();
---

<section>
  <div>
    <!-- {blogImage} -->
    <img src={image} alt={imageDesc} />
    <div>{desc}<span>{blogger}</span></div>
  </div>
</section>
