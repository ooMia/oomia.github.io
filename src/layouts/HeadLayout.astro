---
import { $ } from "#lib/tistory/globals";

const pageTitle = $.page.title.toValue();
const title = $.title.toValue();
---

<head>
  <meta charset='utf-8' />
  <meta name='viewport' content='width=device-width, initial-scale=1.0' />
  <title>{pageTitle} — {title}</title>
</head>
